diff --git a/node_modules/@openid4vc/utils/dist/index.js b/node_modules/@openid4vc/utils/dist/index.js
index 2b6653e..206f3f1 100644
--- a/node_modules/@openid4vc/utils/dist/index.js
+++ b/node_modules/@openid4vc/utils/dist/index.js
@@ -341,9 +341,13 @@ Cause: ${cause?.message}`);
 
 // src/fetcher.ts
 var defaultFetcher = fetch;
+
 function createFetcher(fetcher = defaultFetcher) {
-  return (input, init, ...args) => {
-    return fetcher(
+  return async (input, init, ...args) => {
+    console.log("############### input #################", input);
+    console.log("############### init #################", init);
+    console.log("############### args #################", args);
+    const response = await defaultFetcher(
       input,
       init ? {
         ...init,
@@ -353,8 +357,11 @@ function createFetcher(fetcher = defaultFetcher) {
       } : void 0,
       ...args
     ).catch((error) => {
+      console.log("############### fetch error #################", JSON.stringify(error, null, 2));
       throw new FetchError(`Unknown error occurred during fetch to '${input}'`, { cause: error });
     });
+
+    return response;
   };
 }
 function createZodFetcher(fetcher) {
